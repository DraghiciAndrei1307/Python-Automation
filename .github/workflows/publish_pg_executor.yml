name: Publish 'pg_executor' Package (PyPI & GitHub)
on:
  push:
    branches:
      - main
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Get Code
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install setuptools wheel twine build
          pip install -r requirements.txt
      - name: Build and Publish 'pg_executor' package to PyPi
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
        run: |
          cd pg_executor
          python -m build
          twine upload dist/* --verbose
      - name: Create GitHub Release for 'pg_executor' package
        uses: softprops/action-gh-release@v2
        with:
          files: |
            pg_executor/dist/*.whl
            pg_executor/dist/*.tar.gz
          tag_name: v0.1.0
          name: Release v0.1.0
          body: "The official release of the pg_executor_draghici_andrei package"
